# 💈 Telegram-бот для барбершопа
![Python](https://img.shields.io/badge/python-3670A0?style=for-the-badge&logo=python&logoColor=ffdd54) ![SQLAlchemy](https://img.shields.io/badge/sqlalchemy-%23D71F00.svg?style=for-the-badge&logo=sqlalchemy&logoColor=white) 

Профессиональный бот для записи клиентов на стрижку с полноценной базой данных и всеми необходимыми функциями.

## 🚀 Возможности

- ✅ Запись на стрижку через удобный интерфейс
- ✅ База данных с историей всех записей
- ✅ Проверка занятых временных слотов
- ✅ Сохранение данных клиента для быстрой повторной записи
- ✅ Просмотр активных записей
- ✅ Отмена записи клиентом
- ✅ Уведомления барберу о новых записях и отменах
- ✅ Поддержка нескольких услуг с разными ценами и длительностью
- ✅ Логирование всех операций

## 📁 Структура проекта

```
barbershop-bot/
├── bot.py              # Главный файл запуска
├── config.py           # Конфигурация и настройки
├── database.py         # Модели и работа с БД
├── handlers.py         # Обработчики команд
├── keyboards.py        # Клавиатуры бота
├── requirements.txt    # Зависимости Python
├── .env               # Переменные окружения (создать!)
├── .env.example       # Пример .env файла
└── README.md          # Документация
```

## 🛠 Установка

### 1. Клонируйте репозиторий или создайте папку проекта

```bash
mkdir barbershop-bot
cd barbershop-bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Активация на Linux/Mac:
source venv/bin/activate

# Активация на Windows:
venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте свои данные:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
BARBER_CHAT_ID=telegram_id_барбера
DATABASE_URL=sqlite+aiosqlite:///./barbershop.db
```

**Как получить токен бота:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен

**Как узнать свой Telegram ID:**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте ему любое сообщение
3. Он пришлет ваш ID

### 5. Запустите бота

```bash
python bot.py
```

При первом запуске автоматически создастся база данных SQLite.

## 🗄 **База данных (SQLAlchemy Async)**

### **Таблица users**

|Поле|Описание|
|---|---|
|id|PK|
|telegram_id|уникальный ID|
|username|@username|
|full_name|Имя|
|phone|Телефон|
|created_at|Дата регистрации|
|is_blocked|Заблокирован ли|

### **Таблица bookings**

|Поле|Описание|
|---|---|
|id|PK|
|user_telegram_id|ID клиента|
|user_name|Имя|
|booking_date|Дата|
|booking_time|Время|
|service_name|Услуга|
|service_price|Цена|
|duration|Длительность|
|status|active/completed/cancelled|
|created_at|Время записи|
|barber_comment|Комментарий мастера|


### Миграция на PostgreSQL (опционально)

Для production рекомендуется PostgreSQL:

1. Установите PostgreSQL
2. Создайте базу данных
3. Установите asyncpg: `pip install asyncpg`
4. Измените DATABASE_URL в .env:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost/barbershop
```

## 🎯 Команды бота

- `/start` - Приветствие и информация о боте
- `/book` - Начать процесс записи на стрижку
- `/my_bookings` - Посмотреть свои записи
- `/cancel` - Отменить текущий процесс записи

## ⚙️ Настройка

Все настройки находятся в `config.py`:

### Услуги

```python
SERVICES = {
    "classic": {
        "name": "Классическая стрижка",
        "price": 1500,
        "duration": 30,  # минут
        "emoji": "✂️"
    },
    # Добавьте свои услуги...
}
```

### Рабочие часы

```python
WORKING_HOURS = [
    "10:00", "10:30", "11:00", # ...
]
```

### Информация о барбершопе

```python
BARBERSHOP_INFO = {
    "name": "MASTER CUT",
    "address": "ул. Примерная, д. 10",
    "phone": "+7 (999) 123-45-67",
}
```

## 🔧 Расширение функционала

### Добавление новой услуги

1. Откройте `config.py`
2. Добавьте услугу в словарь `SERVICES`:

```python
"premium": {
    "name": "Премиум стрижка",
    "price": 3000,
    "duration": 60,
    "emoji": "👑"
}
```

### Изменение рабочих часов

Отредактируйте список `WORKING_HOURS` в `config.py`

### Добавление новых команд

1. Создайте обработчик в `handlers.py`:

```python
@router.message(Command("stats"))
async def cmd_stats(message: Message):
    # Ваша логика
    pass
```

2. Обработчик автоматически зарегистрируется

## 📝 Логи

Бот записывает логи в консоль:
- Инициализация БД
- Запуск бота
- Ошибки отправки уведомлений

Для записи в файл добавьте в `bot.py`:

```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()
    ]
)
```

## 🐛 Отладка

### Проблема: Бот не отвечает

1. Проверьте токен в `.env`
2. Убедитесь, что бот запущен
3. Проверьте логи в консоли

### Проблема: Не приходят уведомления барберу

1. Проверьте BARBER_CHAT_ID в `.env`
2. Убедитесь, что барбер начал диалог с ботом (отправил /start)

### Проблема: Ошибки БД

1. Удалите файл `barbershop.db`
2. Перезапустите бота (БД создастся заново)

## 📈 Production

Для деплоя на сервер:

### Systemd Service (Linux)

Создайте файл `/etc/systemd/system/barbershop-bot.service`:

```ini
[Unit]
Description=Barbershop Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/barbershop-bot
Environment=PATH=/path/to/venv/bin
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable barbershop-bot
sudo systemctl start barbershop-bot
```

### Docker (опционально)

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

## 📄 Лицензия

MIT License - используйте свободно в коммерческих проектах.

## 💬 Поддержка

[![Telegram](https://img.shields.io/badge/Telegram-0088CC?style=for-the-badge&logo=telegram&logoColor=white)](https://t.me/gortlt)

---

Сделано с ❤️ для барберов
